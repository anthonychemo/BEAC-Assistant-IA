# Chunking BEAC Assistant

Ce dossier contient le code de segmentation des textes extraits avant vectorisation.

- `chunking.py` : fonctions de normalisation et de découpage en chunks.

## Utilisation

Le script `ingest_to_pgvector.py` dans `data_pipeline/Vectorization` importe ces fonctions.
