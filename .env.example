# PostgreSQL connection for the RAG pipeline
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=beac_rag

# Ollama models
OLLAMA_MODEL=llama2
OLLAMA_EMBED_MODEL=BAAI/bge-m3

# Local server port
PORT=3000
